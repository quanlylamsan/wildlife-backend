# MongoDB Atlas URI (bạn thay bằng URI thật)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/wildlife?retryWrites=true&w=majority

# JWT secret key (tuỳ chọn)
JWT_SECRET=your_jwt_secret_key

# Cổng chạy local (Render sẽ tự cấp)
PORT=5000
